import { TrackerService } from '../service/tracker.service';
export interface IBusyContext {
    message: string;
}
export declare class BusyContainerDirective {
}
export declare class BusyComponent {
    private config;
    private tracker;
    wrapperClass: string;
    private busyContainer;
    constructor(config: any, tracker: TrackerService);
    isActive(): boolean;
}
